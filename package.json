{
  "name": "fashion-tee-agent",
  "version": "3.0.0",
  "description": "AI-powered fashion product aggregator with self-healing capabilities",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "start:test": "node src/index.js --test",
    "test": "node --experimental-vm-modules node_modules/.bin/jest --passWithNoTests 2>/dev/null || echo No tests yet",
    "validate": "node -e \"require('./src/config'); require('./src/config/sites'); console.log('✅ Config valid')\"",
    "audit:prod": "npm audit --omit=dev"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.30.0",
    "airtable": "^0.12.2",
    "axios": "^1.7.9",
    "cheerio": "^1.0.0",
    "dotenv": "^16.4.7",
    "node-cron": "^3.0.3",
    "openai": "^4.104.0",
    "puppeteer": "^24.0.0",
    "puppeteer-extra": "^3.3.6",
    "puppeteer-extra-plugin-stealth": "^2.11.2",
    "winston": "^3.17.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "overrides": {
    "minimatch": "^10.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}